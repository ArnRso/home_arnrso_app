{% extends 'base.html.twig' %}

{% block title %}{{ 'tool.tampon_tracker.title'|trans }}{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">{{ 'tool.tampon_tracker.title'|trans }}</h2>
                </div>
                <div class="card-body">
                    <div class="mb-4">
                        <h5>{{ 'tool.tampon_tracker.current_status'|trans }}</h5>
                        {% if currentStatus == 'inserted' %}
                            <div class="alert alert-success">
                                <strong>{{ 'tool.tampon_tracker.status'|trans }}:</strong> {{ 'tool.tampon_tracker.status_inserted'|trans }}
                            </div>
                        {% elseif currentStatus == 'removed' %}
                            <div class="alert alert-warning">
                                <strong>{{ 'tool.tampon_tracker.status'|trans }}:</strong> {{ 'tool.tampon_tracker.status_removed'|trans }}
                            </div>
                        {% else %}
                            <div class="alert alert-info">
                                <strong>{{ 'tool.tampon_tracker.status'|trans }}:</strong> {{ 'tool.tampon_tracker.status_no_data'|trans }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="row g-2 mb-4">
                        <div class="col-6">
                            <form method="post" action="{{ path('app_tool_tampon_tracker_toggle') }}">
                                <input type="hidden" name="action" value="inserted">
                                <button type="submit" class="btn btn-success w-100" {% if currentStatus == 'inserted' %}disabled{% endif %}>
                                    {{ 'tool.tampon_tracker.insert'|trans }}
                                </button>
                            </form>
                        </div>
                        <div class="col-6">
                            <form method="post" action="{{ path('app_tool_tampon_tracker_toggle') }}">
                                <input type="hidden" name="action" value="removed">
                                <button type="submit" class="btn btn-warning w-100" {% if currentStatus == 'removed' or currentStatus is null %}disabled{% endif %}>
                                    {{ 'tool.tampon_tracker.remove'|trans }}
                                </button>
                            </form>
                        </div>
                    </div>

                    {% if events|length > 0 %}
                        <div class="mt-4">
                            <h5>{{ 'tool.tampon_tracker.history'|trans }}</h5>
                            <div class="list-group">
                                {% for event in events %}
                                    <div class="list-group-item">
                                        <div class="d-flex w-100 justify-content-between">
                                            <span>
                                                {% if event.action == 'inserted' %}
                                                    <span class="badge bg-success">{{ 'tool.tampon_tracker.action_inserted'|trans }}</span>
                                                {% else %}
                                                    <span class="badge bg-warning">{{ 'tool.tampon_tracker.action_removed'|trans }}</span>
                                                {% endif %}
                                            </span>
                                            <small class="text-muted">{{ event.createdAt|date('Y-m-d H:i:s') }}</small>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
